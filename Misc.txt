# Step 1: Build first (creates the DLLs)
dotnet build ImageGen/ImageGen.csproj -c Release

# Step 2: Then pack (creates the NuGet package)
dotnet pack ImageGen/ImageGen.csproj -c Release --output nupkgs